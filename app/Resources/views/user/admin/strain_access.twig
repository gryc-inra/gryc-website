{# app/Ressources/views/admin/user/edit-rights.html.twig #}
{% extends 'base.html.twig' %}

{% block metaTags %}
    <meta name="robots" content="noindex, nofollow">
{% endblock %}

{% block title %}
    Edit {{ user.username }} - {{ parent() }}
{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li class="breadcrumb-item"><a href="{{ path('admin_index') }}">Administration</a></li>
    <li class="breadcrumb-item"><a href="{{ path('user_index') }}">User list</a></li>
    <li class="breadcrumb-item active">{{ user.username }} rights</li>
{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h2>{{ user.username }} <small>{{ user.firstName }} {{ user.lastName }}</small></h2></div>
        <div class="card-body">
            {{ form_start(form) }}
            <div id="admin_user_rights">
                {{ form_errors(form) }}
                <div class="form-group">
                    {% for species in speciesList %}
                        <div class="col-sm-10 col-sm-offset-2">
                            {% spaceless %}
                            <fieldset>
                                <legend>{{ species.scientificName }} <small><a class="checkAllStrains" data-species="{{ species.slug }}" href="#">All</a>/<a class="uncheckAllStrains" data-species="{{ species.slug }}" href="#">None</a></small></legend>
                                    {% for strain in species.strains %}
                                        {% set key = strain.id %}
                                        {% if strain.isPublic %}
                                            {{ form_widget(form.strains[key], {'disabled': true, 'attr': { 'data-name': species.slug } } ) }}
                                        {% else %}
                                            {{ form_widget(form.strains[key], { 'attr': { 'data-name': species.slug } } ) }}
                                        {% endif %}
                                    {% endfor %}
                            </fieldset>
                            {% endspaceless %}
                        </div>
                    {% endfor %}
                </div>

                {{ form_rest(form) }}
            </div>

            <button type="submit" class="btn btn-primary">Save changes</button>

            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
