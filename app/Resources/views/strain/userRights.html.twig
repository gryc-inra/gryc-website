{% extends 'base.html.twig' %}

{% block title %}
    User's authorizations for {{ strain.name }}
{% endblock %}

{% block breadcrumb %}
    {{ parent() }}
    <li><a href="{{ path('admin_index') }}">Administration</a></li>
    <li><a href="{{ path('strain_list') }}">Strains list</a></li>
    <li class="active">User's authorizations for {{ strain.name }}</li>
{% endblock %}

{% block body %}
    <h1>User's authorizations for {{ strain.name }}</h1>

    {{ form(form) }}
{% endblock %}
